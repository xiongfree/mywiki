# 推荐系统的算力兑现路径：从利用率到 IO 的四层硬件约束

> 推荐系统的架构判断不能脱离硬件单独谈——模型规模能放大到哪，取决于硬件兑现了多少；硬件的约束又反过来决定架构往哪收敛。这种双向牵制，正是 Scaling 从一开始就得和 Infra 工程侧一起 co-design 的原因。本文是 `rec_model_scaling_infra.md`（下称 Scaling&Infra）的另一维度补充。Scaling&Infra 自上而下：从 2025-2026 年 25 篇工业工作出发，整理出六层演进体系与七个统一主题，硬件指标在那里是分析工具。本文自下而上：从 GPU 的物理约束出发，经利用率、计算强度、显存、IO 四层逐级向上推到样本组织，回答同一个问题的另一半——**硬件为什么只奖励那些做法**。
>
> 素材声明：本文的硬件层素材主要来自作者平时的文献阅读、工作积累与深入思考（原始版本发布于知乎"推荐系统的算力突围"系列，一篇总览 + 四篇正文，链接见附录 A），包含一手工程实测与线上 AUC 经验；对照部分引用的论断以 Scaling&Infra 为准，数值口径注意事项统一列在附录 B，跨文档引用数字前请先对齐口径。

## 0. 硬件约束是一条因果链，四层缺一不可

Scaling&Infra 主题 1 给出过结论：所有优化的根源是 GPU 没被喂饱，碎片化的小算子让 MFU 只有个位数。这个结论自上而下看是 25 篇论文的共同规律，自下而上则是四层串联的因果链：

```text
范式 GEMM 化（吃到 Tensor Core）
  → 提升算术强度，跨过 Roofline 拐点（进入 compute-bound）
    → 优化显存（给 batch 和序列腾出空间）
      → 优化 IO（让 GPU 不空等数据）
```

四层是串联的：任何一层不通，堆卡只是增加空转成本。业界的普遍状态是卡在最前面——模型范式还是"小卡遗产"（Embedding 查表 + 短序列 + 小矩阵 MLP），算术强度普遍低于 10 FLOPs/Byte，处在 memory-bound 最左端。觉得卡数不够的系统，应先核查 Tensor Core 口径的利用率（总体 TCA / MFU），而不是看板上的 GPU Utilization——那个数字恰恰最该忽略（见 1.1）：多数情况不是算力不足，而是算力从未被兑现。

这条因果链不是理论推演，LLM 已完整走过一遍：从主算子 AI 仅几十到一百、多数训练 memory-bound，到主循环 compute-bound、吞吐随 Tensor Core 峰值缩放，靠的正是范式 GEMM 化、算子与精度改造、显存与 IO 工程的逐层打通（提升史见 2.3）。推荐系统与 LLM 的差距不在卡，在四层中每一层的完成度。

这四层给出的不只是一套硬件知识，更是一套可以直接拿来判断"某个模型设计能不能兑现算力"的依据，也是本文能跟 Scaling&Infra 逐条对上号的原因：后者说"接口要统一成一个交互 Block"，对应的是第一层范式 GEMM 化能不能吃到 Tensor Core；说"长序列这条路走得通"，对应的是第三层显存能不能跟上 batch 和序列长度；说"特征工程这条老路快到头了"，对应的是第四层 IO 跟不上时，堆多的特征会挤占带宽、抢占梯度。矩阵维度够不够（对应第二层算术强度能不能过 Roofline 拐点）、序列长了会不会先被显存挤死、特征堆多了梯度会不会被抢占，都能在方案定稿前算出来，不用等训练跑起来、显存爆了或者吞吐上不去才回头改设计——省下来的是一轮返工的时间和一批卡的空转成本。第 5 节给出逐条对照。

## 1. 第一层：利用率的正确读法

### 1.1 利用率指标：只有 Tensor Core 口径能跨系统比较

性能看板上最常见的 GPU Utilization，统计的是采样周期内"至少有一个 SM 在执行指令"的时间占比——这个指标粒度很粗：只要芯片上有任何一小块在动，就算在忙。所以 GPU 利用率 80% 以上，完全可能对应计算引擎大量空闲。

往下一层看 SM 内部，是 SM Activity（SMA）：至少一个线程在发射指令就算活跃，哪怕这个线程只在做 Load/Store（搬数据，不做乘加运算），SM 也计满。所以 SMA 100% 时，算力发挥可能不到 10%——忙的是数据搬运，不是计算。

再往下看 SM 里真正做乘加运算的部件，才是 Tensor Core。它有两个口径：TCU（Tensor Core Utilization）看时间，即采样周期内 Tensor Core 工作的时间占比；TCA（Tensor Core Active）看空间，即执行指令时实际活跃的运算单元比例。两者会一高一低：TCU 高 / TCA 低，说明算子占着时间长但矩阵小、分块不合理；TCU 低 / TCA 高，说明单次执行满载但被非 Tensor Core 算子挤占了时间。系统报表里的"总体 TCA"是时间加权值，约等于 TCU × TCA。

这三层（整机→SM→Tensor Core）看的是同一块芯片，但粒度越来越细，数字也越来越"不好看"。原因是硬件本身的算力就高度集中在 Tensor Core 上：H100 上 Tensor Core 的半精度算力约为 CUDA Core FP32 算力的 15 倍，总算力的九成以上都在这里。所以"GPU 算力兑现了多少"这个问题，本质上问的是 Tensor Core 忙不忙，而不是整机忙不忙——GPU Utilization 只回答"芯片上有没有东西在跑"，从不回答"跑的是不是真正在算数的那部分"。这也是为什么比较不同系统的算力兑现水平时，只能认 Tensor Core 口径，GPU Utilization 和 SMA 数字再高都不能拿来比。

MFU 是唯一可以跨系统比较的总账指标：实际 FLOPs 除以硬件理论峰值。它与其他指标的关系是：

```text
MFU ≈ 总体 TCA × η    （η ∈ (0,1]，由算术强度不足与流水线空洞决定）
```

一个类比：TCA 是"多少台机器有人上岗"，η 是"上岗的机器有多少时间在加工"。原料供应不足（算术强度低）或流水线卡顿（pipeline 空洞）时，机器都站在那里，产出却很少。这也解释了为什么总体 TCA 高的同时 MFU 可以很低——两者之间隔着一个 η，而 η 正是第二层 Roofline 的主角。

### 1.2 精度门槛：不满足就无法触发 Tensor Core

Tensor Core 不是默认开启的。它要求特定的数据精度类型，而大量推荐系统仍在 TensorFlow 1.x 上运行——不支持 TF32，未做混合精度改造时矩阵运算全部由 CUDA Core 以 FP32 执行，TCA 为 0。以 H100 为例，FP32 的 CUDA Core 算力约 67 TFLOPS，Tensor Core 半精度约 989 TFLOPS（SXM dense 口径），相差约 15 倍。TCA 为 0 意味着第一层的全部优化无从谈起。存量系统的第一步是让计算图跑在支持 TF32/FP16/BF16 的框架和精度下——可以是升级到 TF2.x 并开启 TF32，可以做混合精度改造使用 FP16/BF16，也可以直接迁移到原生支持这些精度的 PyTorch，三条路殊途同归：让矩阵运算落到 Tensor Core 而不是 CUDA Core。这是一次性的门槛成本，也是后续所有优化的前提。
<img width="800" height="480" alt="image" src="https://github.com/user-attachments/assets/7f1cd3ac-b34d-4dd8-8826-8c80d105d981" />


### 1.3 维度对齐：为什么矩阵形状要凑 16、128 的倍数

维度规则不是经验口诀，而是从 GEMM 在硬件上的执行模型推出来的。执行结构分四级：SM 是 GPU 的核心，内含寄存器文件、共享内存、调度器与 Tensor Core；Warp 是 32 线程的最小调度粒度，同一 Warp 同时执行同一条指令；Block 运行在单个 SM 上（一个 SM 可同时跑多个 Block），Block 内的 Warp 经共享内存协作；Tensor Core 是 SM 内的矩阵运算单元，被 Warp 调用。一个大 GEMM C[M,N]=A[M,K]×B[K,N] 会被逐级拆解：输出矩阵先切成 Block tile（常为 128×128 或 256×128），每个由一个 Block 独立完成；Block tile 内再切 Warp tile（如 64×64 或 32×64）；Warp 内由 Tensor Core 指令完成 micro-tile 乘加，线程各自持有寄存器 fragment，累加少量元素后合并成完整输出。

<img width="800" height="480" alt="image" src="https://github.com/user-attachments/assets/c5728197-4524-43c7-9229-7b7129bf98de" />

与执行结构并排的是存储层次：寄存器线程私有、访问最快但容量小，寄存器不足时数据溢出到更慢的层次，直接拖累算力；共享内存与 L1 位于 SM 内、由 Block 内线程协作使用，是 tile 数据复用的主战场；L2 由全体 SM 共享，充当全局缓冲；HBM 显存容量与带宽最大，延迟也最高。tile 数据的装载路径固定：Block 级 tile 从显存读进共享内存，Warp 级 tile 从共享内存读进寄存器，寄存器 fragment 参与 Tensor Core 运算。计算单元决定理论算力上限，存储层次决定这个上限能否被触发——大规模 Embedding Lookup 难以在片上复用数据、只能反复访问显存，正是推荐系统 memory-bound 的微观形态。下面三条规则，全部由这套拆解与装载机制导出。


过了精度门槛，矩阵维度决定 Tensor Core 能否高效执行。三条规则背后是同一个区分：M、N 决定切出多少个 tile、能不能填满 SM，是"数量"问题；K 决定每个 tile 内部循环多少轮 micro-tile 乘加、数据被复用几次，是"深度"问题。前两条规则管 M/N，最后一条管 K。

<img width="400" height="480" alt="image" src="https://github.com/user-attachments/assets/9b05ddf9-c729-4ef9-8a4b-51822617a52e" />

**M/N 取 128 或 256 的倍数（M/N 方向，tile 切分本身）。** 

tile 是硬件调度的最小计算容器，尺寸固定（计算库的 thread block tile 通常为 128×128 或 256×128，访存事务按 256B 对齐，对应 128 个 FP16 元素），矩阵哪怕只占用一个 tile 的一角，也要按整个 tile 的算力和显存开销付费——这就是为什么不满块会 padding、会浪费。具体浪费多少可以算，以 QK^T 的一个头为例：100×100 的矩阵 M、N 方向都不足 128，向上取整只能分到 1 个 128×128 的 tile，但这个 tile 的计算容量是 128×128=16384，实际有效数据只有 100×100=10000，浪费比例 = (16384-10000)/16384 ≈ 39%。QK^T 是 B×H 个 [L,d_h]×[d_h,L] 的小 GEMM 各自独立计算，每个头都按同样的方式补齐到 128×128、浪费同样的 39%，这份浪费不会随 B 或 H 变大被摊薄，而是被重复执行 B×H 次，这就是"与 B、H 无关"的含义。

**tile 总数尽量是 SM 数的倍数（M/N 方向，tile 数量能否填满整卡）。** 

SM 是负责并行执行的物理单元，一轮最多能同时处理"SM 数量"个 tile，tile 总数凑不整这个数，多出来的一小部分就必须单独排到下一轮——多跑一轮的时间成本，和这一轮里到底有多少 tile 无关。SM 数量（A100 为 108，H100 SXM 为 132）决定一轮并行能装多少 tile。这条规则同样约束 M/N：256×128 的 Block tile 只切分 M、N 两个方向，tile 总数由 M、N 决定，K 不参与这次切分——K 只决定每个 Block 内沿 K 方向要做多少轮 micro-tile 累加，不影响 tile 数量，下面的计算全程用不到 K。以 M=2304、N=1664、按 256×128 分块为例：tile 按网格排列，M 方向切出 M÷256 = 2304÷256 = 9 个 tile，N 方向切出 N÷128 = 1664÷128 = 13 个 tile；108 个 SM 一次并行处理 108 个 tile，相当于网格里的 108 个格子，按 M 方向 9 个一列排布，108÷9 = 12，即一轮能装下 12 列（每列铺满 M 方向全部 9 个 tile）；每列占 N 方向 128（Block tile 的 N 边长），12×128 = 1536，即这一轮恰好能配的 N 值上限。N=1664 比这个上限多出 128（正好 1 列），也就是总 tile 数 9×13=117 个：第一轮 108 个 SM 处理满 108 个 tile（12 列），全部忙碌；剩下 9 个 tile（第 13 列）要单独再跑一轮，这一轮里只有 9 个 SM 在干活，其余 99 个空等，tile 数只多了 9 个（占总数不到 8%），执行时间却接近翻倍。这就是尾波（tail wave）：极端情况下 107 个 SM 陪 1 个 SM 跑完最后一轮。

**维度须为 16 的倍数（K 方向，tile 内部的复用深度）。** 

Tensor Core 执行 MMA 指令时一次只能吃进 16×16×16 大小的数据块（三个维度对应 M/N/K 三个方向的 micro-tile 切片），这是比前两条 tile 级更细一层的指令级粒度，实际约束的是 K：K 不被 16 整除时，Warp 内沿 K 方向做 micro-tile 乘加会因为凑不满一个 16 的切片而在指令内部补零、padding，浪费的是指令级算力，与前两条 M/N 方向的 tile 切分是两回事。实测 A100 上 K=4095 对比 K=4096：TF32 慢 64%，FP16 慢 190%，BF16 慢 197%——只差一个元素，耗时翻倍。

落到 Transformer 的推荐改造上，实操清单是：序列长度 L 取 128 的倍数（条件不允许时至少 16 的倍数）、model_dim 取 128 的倍数、batch size 在矩阵小时精算 tile 数是否凑满 SM、head_dim 取 64 或 128。前三条对应 M/N 方向的两条规则，最后一条对应 K 方向：

- **L 取 128 的倍数**，对应"M/N 取 128 或 256 的倍数"——L 在 Attention 的 QK^T 等 GEMM 里充当 M 或 N 维度，不满 128/256 的 Block tile 就有量化浪费（前文 100×100 QK^T 在 128×128 tile 下浪费约 39% 即是此例）；退而求其次要求的"至少 16 的倍数"，对应的是最后一条更基础的"维度须为 16 的倍数"，避免 Warp 内 micro-tile 出现 padding。
- **model_dim 取 128 的倍数**，同样对应 M/N 方向的 tile 对齐：作为 QKV、FFN 等 GEMM 里的 d，它出现在 M/N 方向（如 [BL,d]×[d,3d] 中 d 是 N），要满足 tile 对齐；它也直接决定算术强度（2.2 节 AI≈1.5·d/b），128/256 的隐层维度够不到 295 拐点，所以这条不只是凑对齐，还要求往大了取。
- **batch size 精算 tile 数**，对应"tile 总数尽量是 SM 数的倍数"——batch 与 L 相乘构成 M 维度（即 2.2、2.4 节记号中的 BL），M 方向 tile 数由 M 除以 Block tile 边长决定，凑不满 SM 数就会出现尾波（tail wave），即前文"107 个 SM 陪 1 个 SM 跑完最后一轮"的场景。
- **head_dim 取 64 或 128**，对应的是 K 方向的深度对齐——它在 QK^T 中是被消去的内维度 d_h，64、128 都是 16 的整数倍，不对齐会像 K=4095 对比 4096 那样在 Warp 内 micro-tile 累加时 padding；取 64 或 128 这两个具体值还叠加了第二层的算术强度门槛（2.2 节 d≥400/375 的反解），是硬件对齐与过 Roofline 拐点两层约束共同筛出来的取值。



### 1.4 对照 Scaling&Infra

主题 1 的 MFU 表（RankMixer 4.5%→45%、Kunlun 17%→37%、OneRec 极低→23.7%/28.8%）在这一层有了机制解释：碎片化小算子同时压低 TCA（矩阵小、tile 不满）和 η（算术强度低、kernel 启动开销占比高），统一成大 GEMM 是同时修复两者。4.4 节提出的"GEMM 统一度"指标，其硬件微观基础就是本节的 tile 与尾波分析——统一度高意味着维度整齐的大矩阵：M/N 够大能切出足够 tile 填满 SM，K 够深则同一份从显存读入共享内存的 Block tile，能在 K 方向被更多轮 micro-tile 乘加反复复用（该复用机制的算术强度推导见 2.1）。4.4 的"硬件对齐分层筛选"（先筛 8/16 对齐，再做离散消融）落地到本节就是那份实操清单。

## 2. 第二层：Roofline 与计算强度

### 2.1 Roofline 公式：算力和带宽谁是瓶颈

Roofline 模型把计算能力与存储带宽的关系压进一条公式：

```text
Performance = min(Peak FLOPs/s, AI × Memory Bandwidth)

加速器强度 = 【加速器某精度理论算力FLOP/s】 /显存/主存峰值带宽【Bytes/s】  → H100 约 295 FLOP/byte （BF16/FP16 Tensor 稠密 989 TFLOP/s、带宽 3.35 TB/s ）
算术强度AI = FLOPs / Bytes        → 该操作每搬运 1 字节能做多少 FLOP
```

AI（Arithmetic Intensity，算术强度）= 计算量 / 访存量，单位 FLOPs/Byte。（某个 kernel/算子/实现，搬这些字节做了多少浮点）
加速器强度AI*：硬件固有的算力/带宽比，也是Roofline拐点；AI 低于拐点时性能被带宽卡住（memory-bound），高于拐点时被算力卡住（compute-bound）。
H100 SXM 的拐点约 295 FLOPs/Byte（989 TFLOPS ÷ 3.35 TB/s）。推荐系统的普遍状态是 AI 低于 10，处在最左端——这意味着买了 H100 的算力，实际用到的只有它的显存带宽。

<img width="600" height="480" alt="image" src="https://github.com/user-attachments/assets/9b935e0b-1bbf-4cfa-a1c1-0f9642a8d892" />
Roofline 图直观地展示了算术强度与性能的关系：

X 轴：算术强度（每个"切片"对应一个特定算法）
Y 轴：实际达到的 FLOP/s
每条分段线性曲线：一个特定的硬件平台（H100、B200 等）
转折点（kink）：该硬件的加速器强度——转折点左侧是 memory-bound 区域（斜率上升），右侧是 compute-bound 区域（水平天花板）
Percy 解释："如果你的操作在转折点左侧，说明算术强度不够高，实际 FLOPs 远低于硬件的峰值能力。只有当算术强度超过转折点，你才能接近峰值 FLOP/s。"

```text
MFU 与 Roofline 的关系：
MFU = min(1, 算术强度 / 加速器强度)
```
这就是 MFU 通常只有 0.5 左右的原因——很多操作的算术强度低于加速器强度，导致 GPU 的计算单元在"空转等待数据"。

Roofline只回答"AI 落在拐点哪一侧"，不回答"AI 由什么决定、怎么才能推高"——下面两条结论回答这两个问题。

**结论一：矩阵三个维度要同步放大，单独加深某一维收益会很快归零。** 对 GEMM（A[M,K]×B[K,N]→C[M,N]，精度字节 b）：

```text
AI ≈ 2MNK / b(MK + KN + MN)
```

分子 2MNK 是三个维度的乘积，分母 b(MK+KN+MN) 是两两乘积之和。若 M、N、K 同步放大 s 倍，分子放大 s³ 倍，分母每项放大 s² 倍，AI 净增长 s 倍；若只放大 K（M、N 不变），分子里的 MNK 项与分母里的 MK、KN 两项增速相同，AI 会收敛到一个只由 M、N 决定的常数，K 再大也推不动 AI。这就是想把 AI 推过拐点该往哪个方向使力：M、N、K 要同步放大，单独加深一维不够。

硬件层面，M、N 同步放大对应 tile 数量——越大能切出的 tile 越多，一次装入共享内存的数据服务的计算量越大；K 放大对应 tile 内部的复用深度——每次从显存读入共享内存的数据，会在 K 方向被沿着 16×16×16 的 micro-tile 反复做乘加，K 越大复用轮数越多，读一次、算很多次，访存成本被摊薄。

**结论二：工程优化只能让实际表现逼近理论上限，不能推高理论上限本身。** AI 分静态与动态两种口径：静态 AI 假设中间结果全部驻留寄存器/共享内存、缓存命中 100%，是仅由 M、N、K、b 这几个模型设计参数决定的理论上限；动态 AI 按运行时性能计数器的真实访存量计算，多出的访存来自中间结果写回、缓存未命中、tile 与指令级 padding，会低于静态 AI。算子融合能把两者拉近——中间结果留在寄存器/共享内存不写回显存，多个小 kernel 合并减少缓存未命中和调度开销，动态 AI 因此逼近静态 AI，但融合改的是"多接近上限"，改不了上限本身。这意味着代入 Roofline 公式判断是否过拐点时，该代入的是动态 AI：用静态 AI 算出"已经过拐点"不代表实际执行真的过了拐点。这一区分贯穿本层和第 4 层的分析。

### 2.2 提升算术强度：加宽比加深更有效

把加宽、加深代入 2.1 节的 AI 公式就能看出两者的本质差异。加宽（增大 d_model、head_dim、FFN 中间层）改变的是单层 GEMM 的 M/N/K 本身——对应 2.1 节结论一里"维度同步放大"的情形，所以同时提升 FLOPs 和 AI；加深（堆更多层）是重复堆叠同样形状的层，单层的 M、N、K 都不变，FLOPs 和访存量按层数同步线性增长，两者的比值（AI）不变，所以只提升 FLOPs，单层 AI 不变。

在 memory-bound 区，Roofline 公式里的性能 = AI×带宽，AI 不变、带宽是硬件常数，性能就不会因为堆层数而提升——即使总 FLOPs 确实堆多了。这就是"反直觉"的地方：直觉上算力投入（FLOPs）越多，产出应该越多，但在 memory-bound 区，有效产出只取决于 AI 和带宽，与 FLOPs 总量无关，多堆的 FLOPs 换不来更多产出，等于白算。**加深在 memory-bound 区是无效投资，这是反直觉但严格成立的结论**。

用 Transformer Base 的两个大算子算具体数字验证这条结论（b=2，BL 为 batch×序列长度）：

- QKV GEMM（[BL,d]×[d,3d]）：BL 远大于 d 时 AI ≈ 1.5·d/b，随 d 线性增长；d 远大于 BL 时饱和于 2·BL/b
- FFN 单次 GEMM（[BL,d]×[d,4d]）：BL 远大于 d 时 AI ≈ 1.6·d/b

对 H100 的 295 拐点反解：QKV 需要 d ≥ 400，FFN 需要 d ≥ 375。**推荐系统常用的 128/256 隐层维度，无论怎么加 batch 和序列长度，AI 都够不到 295**——小维度下 M 维增长收敛到的常数上限太低。这是"统一交互 Block 应该做多大"的第一个硬数字下限，也是对主线 2 的直接支撑：RankMixer 路线把主干做成大维度 MLP-Mixer，不只是结构上的统一，是过拐点的必要条件。

但加宽优先不等于深度无用，上面的结论有适用范围的限定。LLM 的 compute-optimal 研究（Chinchilla）给出近似最优宽深比 d_model∝P^0.4、n_layers∝P^0.3：深度仍贡献建模能力与表达深度，只是在 memory-bound 区其算力增长被访存增长同步抵消；跨过拐点后，加深恢复为有效投资。最优宽深比依赖特征数量、embedding 尺寸、序列长度与任务目标权重，推荐系统需要像 LLM 一样系统性探索自己的宽深边界，而不是套用单边结论。

以上是算术强度在硬件层面的意义（决定 memory-bound 还是 compute-bound）。算术强度还有一层算法侧解读：AI powers AI。更高 AI 的结构，意味着相同访存信息下计算的连接与交互更密集，更能产生智能——Scaling Law 研究中，无论扩参数还是扩 token，高 AI 的 Transformer 都比低 AI 的 LSTM 有更好的 Scaling 曲线。推荐系统持续借鉴 NLP 成果，根源在于用户行为序列与文本 token 序列的定义相似：拉长序列不只是线性提升 AI，也在扩宽可挖掘的知识面。AI 因此不只是省钱指标，也是度量算法潜力能走多远的标尺。

### 2.3 QK^T 的 L² 陷阱：为什么 Flash Attention 是必选项

**结论先给出：没有 Flash Attention，拉长序列这件事本身就跑不通——不是慢，是直接卡死在 memory-bound、性能不会随序列变长而提升；有了 Flash Attention，拉长序列才能真正换来性能提升。前者是能不能跑通的问题，后者才是快不快的问题，Flash Attention 解决的是前者。** 这给 Scaling&Infra 主线 3 的判断提供了独立推导，下面按逻辑层层展开这个结论。

第一层：为什么不做 Flash Attention 之前，拉长序列在数学上走不通。Attention 的 QK^T 不是一个大 GEMM，而是 B×H 个并行的 [L,d_h]×[d_h,L] 小 GEMM：

```text
AI(QK^T) = 2L²d_h / b(2L·d_h + L²)
```

它只依赖 L 和 d_h，与 B、H 无关。L 很大时 AI 极限是 2d_h/b——一个与 L 无关的常数：分子 L²d_h 是平方级增长，分母里的 L² 项同样是平方级增长，两者增速相同，L 再大也推不动 AI。这意味着单纯拉长序列换不来更高的算术强度，无论特征序列还是行为序列，都会一直卡在 memory-bound 区，序列越长、显存访问越多，性能却不会随之提升。

第二层：Flash Attention 怎么解决这个陷阱。它利用 Softmax 平移不变性，把 QK^T、Softmax、乘 V 整体搬到片上 SRAM 分块完成，L×L 注意力矩阵从不落显存，访存从 O(L²) 降到 O(L)：

```text
AI(QK^T | FA) ≈ L/b    （随序列长度线性增长）
```

访存量降到线性级后，AI 不再收敛到常数，而是随 L 线性增长——这正是拉长序列所需要的：序列越长，算术强度越高，才有可能越过拐点进入 compute-bound。这也是为什么 Flash Attention 被称为"可行性前提"：没有它，L²的访存陷阱会让拉长序列这条路在数学上就走不通，不是效率问题，是能不能走通的问题。

第三层：LLM 的算术强度提升史印证了这个判断。GPT-2/3 时代主算子 AI 仅几十到一百 FLOPs/Byte、多数训练 memory-bound；GPT-3.5/PaLM 时代扩 batch、拉长 L、FA v1/v2 出现；GPT-4/LLaMA3 时代长上下文加巨批次，主循环已 compute-bound，吞吐随 Tensor Core 峰值缩放。这条演进路径与上面的推导一致：Flash Attention 出现之后，长序列才真正成为可以持续投入的 Scaling 维度。

### 2.4 Batch Size 的作用被高估：真实门槛在维度，不在 batch

**结论先给出：加 batch 不是没用，是很快没用——真正决定吞吐能不能持续涨的是隐层维度（N、K），不是 batch（对应 M）。** "加 batch 提吞吐"是推荐系统的经验，但这条经验只在 memory-bound 区间成立，且很快撞到收益递减的天花板。

第一层：为什么门槛在维度、不在 batch。这是 2.1 节结论一的直接应用：batch 只对应 GEMM 的 M 维，N、K 是隐层维度、由模型结构决定、与 batch 无关。单独放大 M（N、K 不变）时，AI 会收敛到一个只由 N、K 决定的常数——常数的高低由 N、K 决定，batch 加多少都改不了这个上限。N、K 小，这个上限就低，怎么加 batch 都够不到 compute-bound；N、K 够大，上限才有机会够到拐点。

这个常数具体是多少，把 M→∞ 代入 2.1 节的 AI 公式就能算出来。AI ≈ 2MNK / [b(MK+KN+MN)]，分子分母同除以 M：AI = 2NK / [b(K+N+KN/M)]，M→∞ 时 KN/M 这一项趋于 0，极限就是：

```text
AI 上限 = 2NK / [b(N+K)]
```

QKV GEMM 里 N=3K（3d 是 QKV 把 d 映射到的输出维度，K=d 是输入维度），代入化简：AI 上限 = 2×3K×K / [b×(3K+K)] = 6K² / (4bK) = 3K/(2b)，取 fp16 的 b=2，就是 **AI 上限 = 3K/4**——一个只依赖 K（隐层维度 d）的常数，K 越大上限越高，但 batch 再大也摸不到这个数以上。

第二层：代入具体数字验证，且都很快撞到天花板。K=128（N=384=3K）时，AI 上限 = 3×128/4 = 96，进不了 compute-bound；batch 加到 95（M/L 折算）时吞吐比 batch=18 只多涨 4%，基本可以忽略。换一组更大的隐层维度 K=1024、N=3072，AI 上限抬高到 768，但 batch=5 之后继续加也不再涨吞吐了。两组算例对比的是同一件事：N、K 越大，AI 上限越高，能摸到的 batch 规模也越大，但无论上限高低，摸到之后继续加 batch 都是徒劳——区别只是"多久撞墙"，不是"会不会撞墙"。（注：768 这一算例沿用系列原文不含精度字节 b 的口径——即 AI 上限=KN/(K+N)，比上面推导的 2NK/[b(N+K)] 少了分子的 2 和分母的 b，与前一算例口径不完全统一，此处沿用原文数字，结论不受影响。）

第三层：这个结论在 LLM 侧的印证。LLM 敢用 Gradient Accumulation（把大 batch 拆成多个 micro-batch 累积梯度再更新）正是因为 d 够大时 micro-batch 很小就能到达吞吐极限，拆分 batch 不会掉吞吐——这与上面"batch 摸到上限后加多少都没用"是同一个道理的另一种应用：既然继续加 batch 无益，那把 batch 拆小也不会有损失。

### 2.5 算子融合：减少读写次数，逼近理论算术强度

先回顾 2.1 节的两个概念：静态 AI 是假设访存全部理想（中间结果不落显存、缓存 100% 命中）算出的理论上限，只由 M、N、K、精度决定；动态 AI 是拿真实访存量算出的实际值，会低于静态 AI。
**结论先给出：差距来自算子之间"多余"的一段访存——如果 A、B 两个算子分开执行，A 算完要把结果写回显存，B 再读回来接着算，这一写一读纯粹是因为两者是独立的两次 kernel 调用；融合把 A、B 合成一个 kernel，结果直接留在寄存器/共享内存被 B 接着用，这段本不必要的访存就被消掉，动态 AI 因此逼近静态 AI。** 这是 2.1 节结论二里"工程优化逼近理论上限"的具体实现方式。

**多余访存有三个并列的具体成因**
融合逐一消掉。
其一是中间结果写回再读出，如上所述。
其二是缓存未命中：分开执行时，A 算完后 GPU 缓存里留的是 A 的数据，等 B 被调度执行时往往已被其他线程块冲刷掉，B 读数据要重新从显存拉取；融合后 A、B 在同一个 kernel 里连续执行，数据还留在缓存里就被直接用上。
其三是 kernel 启动与调度开销：每次调用 kernel，CPU 都要发一次启动指令，这个延迟在几到几十微秒量级，与计算量无关，对激活、归一化这类小算子可占总耗时的 30-50%；连续调用多个小 kernel 还会造成流水线泄露——GPU 流水线需要连续喂数据才能填满并发挥满载吞吐，kernel 太小、前一个还没填满流水线就切换到下一个，相当于每次都"半载起步就结束"，吞吐率因此下降。
QKV 三个独立 GEMM 融合为 [d,3d] 单次 GEMM 是典型案例：共享输入 X 从读 3 次降到读 1 次，访存开销 3.0x→1.2-1.3x（没能精确降到 1/3 是因为大 GEMM 仍有 padding 等开销），动态 AI 1.0x→2.5-3.0x。
第三个成因还有一个互补解法：CUDA Graphs，把一整段 kernel 调用序列录制成一张"图"，CPU 只需一次调用即可让 GPU 按图批量执行，启动次数从 N 次降为 1 次，消掉每次单独启动之间的往返延迟；kernel 小、调用频繁的推荐场景收益尤其明显，可与 Fusion 叠加使用。

**融合分三级，区分标准是"谁来决定融合、决定权在哪"：**
编译器自动融合（XLA / TorchInductor）由编译器扫描计算图自动判断，透明但不可控——融合规则是黑盒，版本升级或写法稍变就可能不再触发；
显式融合算子（Flash Attention 这类）是手写的融合版 kernel，可控但维护成本高——要跟着硬件架构升级重新调优甚至重写，因此优先复用 LLM 生态已有的开源算子；
写法级触发融合（将 `gelu(x @ w + b)` 保持在一行）介于两者之间，不用手写 kernel，只是用写法给编译器的自动融合"提示"。

### 2.6 推荐系统侧的融合实践

上一节讲的算子融合机制是执行层的通用技术，这一节看这个"融合"思路在推荐系统里的两处具体落地：一处是执行层的 concat 融合，一处是特征设计层的 side-info 与 ID 融合选型。

**concat 融合与 embedding 维度对齐。** 任何 concat 都应审视是否需要融合——concat 本身不做任何计算，是零计算操作，AI 因此极低（分母访存量不为零、分子计算量接近零），却仍然产生一次显存写和读；XLA 不保证自动融合 concat 与后续 GEMM，稳妥做法是用 batched matmul 在一个 kernel 内完成多 GEMM 相加，用计算代替 concat 这一步显存搬运（K 维均为 16 倍数、M×N 够大时，性能与大 GEMM 几乎无差）。side-info 的 embedding 维度也应取 16 的倍数：这是 2.1 节 K 需为 16 倍数那条规则在这里的直接应用——为省存储选 4/8 维看似省了参数量，但会在 Tensor Core 的 16×16×16 指令级 padding 上补零，padding 造成的浪费比省下的那点存储更大。

**side-info 与 ID 的融合方式选型，是算法-系统 Co-Design 在特征层的完整案例。** 三种方案：其一，side-info 与 ID 取同维 embedding、逐元素相加；其二，各自按需选维度、独立线性映射到同维再相加；其三，按信息容量选维度、concat 后线性映射到 token 维度。其二与三实现不同，考虑 fusion 后殊途同归，即前述 batched matmul 的一次 kernel 多 GEMM 相加。系统侧权衡在存储与带宽：其二/三按信息量入为出，embedding 存储省；其一可以在 GPU 外（PS 侧或 EP 本地）先做 sum pooling，省掉网络带宽与 GPU 的多次 GEMM——side-info 表更稠密、行数远少于 ID 数，同维浪费的存储并不大。瓶颈在带宽还是存储，决定选哪边。

算法侧还有一个容易被忽视的收益：语义锚点。系列原文的表述是"同维相加后 embedding 接收的梯度是 1/m"（m 为相加项数）——严格按 sum pooling 求导，每一路的梯度系数应为 1 而非 1/m，1/m 更准确对应 mean pooling，此处沿用原文说法，核心论点不受这处口径影响：batch 里稀疏 ID embedding 随机初始化的权重本会产生近乎随机的梯度，却跟着稠密的类目/品牌 embedding 走了一步——稀疏侧被稠密侧拉着走，相当于隐式正则：类目/品牌 embedding 把相似 ID 拉近，冷启动与长尾 item 学得更好。其二方案的 Linear 变换能学习 side-info 重要性，算表达能力上的弥补；两种路线表达能力不相上下，选型可让系统瓶颈做主。

### 2.7 对照 Scaling&Infra

主题 1 的"成本算术题"在这里有了定量版本：MFU 4.5% 不是"模型差"，是 AI 低于拐点时性能被带宽钉死，95.5% 的 Tensor Core 在空转。RankMixer"百倍参数、推理延迟不变"是把新增 FLOPs 装进空闲窗口，但这个解释只对 serving 口径成立：推理小 batch 下的时间大头是碎片算子的启动与访存开销，大 GEMM 化既压缩开销又装进新计算，延迟才可能持平；若拿训练 MFU 4.5%→45% 验算（时间不变只能容纳约 10 倍 FLOPs），账就对不上——训练与推理是两本时间账，不能互相验算。Scaling&Infra 4.7.1 对压缩后 Self-Attention 的两种口径分析（非融合实现 AI≈d，融合实现 AI≈O(L) 且与 d 基本无关）与本文 2.3 的推导同构——Scaling&Infra 用它反推压缩比下界，本文给出该推导的源头，两份文档在这条方法上互相验证。Scaling&Infra 4.6 的 gather-GEMM 双缓冲 staging（把随机访存变成异步预取加流水掩盖）与本文 2.5 的 Fusion 同源：都是压缩动态 AI 与静态 AI 的差距，只是对象从"算子间中间结果"换成"gather 输出"。

## 3. 第三层：显存

### 3.1 显存分四块，激活值是占比最大的一块

**结论先给出：训练显存分参数、梯度、优化器状态、激活值四块，前三块随参数量线性增长且量级可预估，激活值才是显存爆炸的真正大头，且远比前三块难估算。**

前三块的估算公式（P 为参数量，以 1B 为例）：
- 参数：FP32 为 4P；混合精度为 6P（FP32 主权重 4P + FP16 计算副本 2P）→ 1B 时 4GB / 6GB
- 梯度：混合精度为 2P → 1B 时 2GB
- 优化器状态（Adam）：8P，且须 FP32 存储 → 1B 时 8GB（动量是梯度的指数滑动平均，训练初期梯度小，半精度存储会精度下溢，更新趋近于零）

三块合计约 4P+2P+8P=14P（1B 时约 14GB），随参数量线性增长、容易预估。

激活值是什么、为什么不同：前向传播时，每一层（Attention、QKV 投影、FFN 等）算出的中间结果——不是最终输出，而是层与层之间传递的那些张量。反向传播要用链式法则算梯度，每一步都要用到对应层前向时的中间结果，所以必须在前向时保留在显存里，一直留到反向用到、算完梯度才能释放。这是它和参数、梯度、优化器状态的本质区别：后三者只跟"模型多大"有关，激活值还跟"这一次跑了多大的输入、多少层同时要保留中间结果"有关——batch、序列长度、层数等运行时配置共同决定了它的大小，同一个参数量的模型，配置不同、激活值可以相差数十倍。

两组独立算例说明这一点（配置不同，不是同一模型的两次测算）：其一，B=1024、H=16、L=1024、FP16 下，单层 Attention Map（即 Attention 这一层要保留的中间结果）即达 32GB；其二，另一组配置（B=512、L=1024、H=8、d=512、4 层）下，4 层合计 Attention Map 32GB、QKV 投影约 6GB、FFN 激活约 18GB，三项相加总量约 60GB。1B 模型在 80GB 卡上 OOM，大概率是激活值而非参数占满了显存。

### 3.2 激活值优化：四种手段各解决哪种显存压力

**结论先给出：四种手段治的是四种不同的病，不能互相替代——Checkpointing 治总量、Gradient Accumulation 治小 batch 的副作用、Flash Attention 治 Attention 这一项的量级、混合精度治所有项的单位存储成本。**

Checkpointing：不是所有中间结果都留着，而是隔几层留一个"存档点"，其余的等反向传播用到时再重新算一遍——用多算一次的算力，换掉不用一直占着显存存所有中间结果的空间。反向传播需要的显存因此从"跟总层数成正比"降到"跟存档点之间隔几层成正比"，适合显存比算力紧张的场景，因为这笔"多算一次"的账划算。

Gradient Accumulation：显存不够时，一个直接的办法是把 batch 调小，但 batch 太小会导致每次算出来的梯度方差大、忽高忽低，模型更新不稳定，收敛变差。Gradient Accumulation 的做法是把一个大 batch 拆成几个小的 micro-batch 分别算梯度，累加起来，凑够原来大 batch 的量之后才更新一次参数——显存上用的还是小 batch 的量，但更新时用的是大 batch 累加后的梯度，收敛效果跟直接用大 batch 一样。

Flash Attention：把 Attention 这一项要保留的中间结果，从"跟序列长度的平方成正比"降到"跟序列长度成正比"。

混合精度：把所有中间结果从 32 位浮点数降到 16 位浮点数存储，数量不变，但每个数占用的存储空间减半，所以总显存也减半。

Flash Attention 怎么做到这一点：不再一次性算出完整的 L×L 大矩阵，而是把 K、V 切成小块，每块单独在 GPU 片上缓存里算完 Softmax、乘 V，再累加进一个总结果——只维护累加的加权和与权重和这两个量，大小只跟序列长度成正比。分块累加要解决一个数值细节：Softmax 要减去最大值再取指数防止溢出，但分块时不知道全局最大值，Flash Attention 的做法是先按当前块的最大值算，遇到更大的值再把已累积的结果按比例缩放对齐——这保证了分块算和一次性算结果完全一致，是 2.3 里长序列可行性前提的数学内核。

混合精度还牵出两个配套问题。一是为什么仍要保留 FP32 主权重：BF16 有效数字位数远少于 FP32（约 2 位十进制精度 vs 约 7 位），参数每步的更新量本身很小——学习率 0.0001、梯度 0.01，更新量是 0.000001，这个数字在 BF16 精度下会被直接舍成 0，训练等于卡住不动；所以训练时始终留一份 FP32 主权重做累加更新，推理阶段才可以直接用 BF16。二是 loss scaling：FP16 数值范围窄，小梯度会下溢变成 0，须先放大 loss 再反向传播；BF16 数值范围跟 FP32 一样大，不需要这一步，这是混合精度优选 BF16 的又一个原因。

### 3.3 并行策略选型：推荐系统为什么优先选数据并行

并行策略分两条路径：模型并行（MP）切模型，数据并行（DP）切数据、模型完整复制到每张卡。TP、PP 都属于 MP，切法不同：TP 横切层内的矩阵运算，通信频繁；PP 纵切层与层之间，存在"流水空泡"（某段算完前后面的卡要空等）。ZeRO 是 DP 的显存优化版，把优化器状态/梯度/参数分片存储、需要时临时组装，1/2 分别对应分片优化器状态、再加分片梯度。

**结论先给出：判断标准很直接——总参数 3B 以内且单卡 80GB 能跑通，就用 DP + ZeRO-1/2，不需要上 TP/PP。** 3B、80GB 这两个数字不是精确阈值，是"单卡能否装下参数+梯度+优化器状态+激活值"这个显存约束的经验估计口径——推荐模型的参数规模通常远低于 LLM，大概率落在这个范围内，可作为快速判断的量级参考。

为什么不需要 TP/PP：TP 切单层、通信频繁，PP 按层切、有流水空泡，两者的复杂度收益比在"参数不大"的推荐模型上都不划算，只有单卡装不下时才值得为它们付出这份复杂度。

DP + ZeRO 具体怎么做：普通 DP 每张卡都要完整保留一份优化器状态、梯度、参数的副本，其中优化器状态（3.1 提到的 Adam 8P）是最大头的一块，每卡重复存一遍等于没有真正分摊这块显存压力。ZeRO 的做法是把这三类状态按卡数切开，每张卡只存自己的分片；但计算时某张卡仍需要完整的参数/梯度才能算，于是在真正用到的那一刻用 All-Gather 把分片临时组装成完整的，用完立刻释放，梯度同步阶段则用 All-Reduce 汇总各卡的梯度分片——省显存和能计算两者兼顾，代价是这个临时组装要走一次卡间通信，128 卡内吞吐损失可控制在 10% 内（该数字为公开文献与社区报告口径，供量级参考，见附录 B）。

还有其他备选但推荐场景多数用不上：优化器侧（AdaFactor 状态从 O(n) 降到 O(√n)、Lion/Sophia 只留一阶动量、优化器状态 offload 到 CPU）这些手段是为参数和状态占用本身很大时准备的，推荐模型参数与状态占用本来不大，选型应以效果为先，不必为此额外引入这些手段。

### 3.4 对照 Scaling&Infra

主线 5（底层基建）的训练侧在本节具体化。更重要的是主题 1 的相变点在这一层有一个代价反转：Checkpointing"以算换存"在 memory-bound 区划算（计算闲着），跨入 compute-bound 后重算成本变成真实算力开销，ULTRA-HSTU 的选择性激活重计算（只重算便宜的、不重算贵的，单层显存 7GB→2.3GB）正是在这条边界上做精细权衡。显存层与第二层是耦合优化：腾出的显存换更大的 batch 和 L，而 2.4 说明只有 d 够大时这笔交换才有效——单维优化显存不会自动带来吞吐。


## 4. 第四层：IO 与样本范式

**结论先给出：这一层 GPU 空等的根源是数据供给跟不上，但最有效的解法不在传输层，而在样本与特征的范式层——这是 4.1 到 4.5 的立论起点。**

推荐系统的 IO 压力尤其大：样本量庞大、特征稀疏、Embedding 参数 TB 级，数据加载、特征访问、参数拉取都在消耗带宽。判断依据：排序模型的结构一直在换（DIN 到 Transformer、MLP 到 TokenMixer），但范式没变——都是序列建模先汇总出用户表征，再和其他特征进特征交叉；结构替换的收益已边际化，剩下能带来增益的方向是样本组织和数据通路本身。

### 4.1 样本组织：从平方级冗余到 user-level training

**结论先给出：point-wise 样本组织带来 impression × 历史 engagement 的平方级冗余；解法是共享粒度一路升级——impression 级到 request 级到 user-level，覆盖存储、IO、计算三层，训练和推理侧共用同一套机制。**

point-wise 下每个 impression 单独生成一条样本：同 request 内的多个 impression 共享同一份用户历史特征，却各自完整存了一遍，这是 impression 维度的冗余；同一用户不同天的样本，历史序列大部分重叠，这是历史 engagement 维度的冗余。两维相乘就是平方级冗余，序列越长，重复占比越高。

共享粒度的升级分三步：先在 request 内共享一条历史序列（消除 impression 维度冗余，改造代价最小）；再把范围扩大到 M 天聚合（M=30，吃到历史 engagement 维度的冗余收益，全量重训成本大幅下降），并按 N 天重新切分以兼容按天增量训练（N=1 即 daily 增量）。到这一步，冗余已经在存储和 IO 层面被压缩到 user-level：一个用户的历史序列只存一份，所有 request 共享同一份存储。

流式训练系统只能停在 request 级共享，攒够 M/N 天再处理的前提在流式场景不成立——这类系统在算力驱动的 Scaling Path 上会持续落后。批式样本重组不是工程偏好，是长序列路线的前提（该判断的适用边界见 5.4）。

存储和 IO 的共享升级到 user-level 后，计算侧的重复计算还要靠模型结构接住：decoder-only 的因果注意力下，历史序列每个位置的 Key/Value 只由它自己和更早的行为决定，不受后面 target 影响，历史 KV 只需算一次，此后每个 target 只用自身 ID 和 side-info 作为 Query 去查这份 KV——计算量从"每个 target 算一遍"降到"整个用户算一遍"，user-level training 由此成立。

推理侧是同一升级的延伸：模型不更新时，Session 内甚至 N 天内的历史 KV 可直接持久化，新行为只补算增量 KV。工程上选 Session Affinity（同一 UID 固定打到同一 GPU 节点）而非分布式 KV 存储——实现更简单、延迟更低。

### 4.2 去记忆性特征：既省带宽，也让梯度不被稀疏特征抢占

**结论先给出：记忆性特征一边在训练动力学上抢占泛化结构的学习机会，一边在 IO 上占带宽却不带来增量信息，两个理由都指向同一个动作——去掉它们。**

记忆性特征（稀疏笛卡尔积交叉、UID 大规模 FFM 交叉、用户 Match 统计特征）本质是记住训练数据里出现过的模式，收敛快、梯度信号强，训练初期会抢占泛化结构（如序列建模）的学习空间——one-epoch 场景尤甚：记忆性部分先收敛，泛化结构学不到东西，冷启动与长尾无解。原作者一手经验：加入这些特征后长期 AUC 下降；目前已去掉笛卡尔积与 Match 特征，下一步动 UID 隐式交叉。

IO 侧是独立的另一层：这些特征占满数据通路带宽，却不随序列拉长带来增量信息，砍掉后给数据通路减负，剩下最大的参数块收敛到物品 ID embedding，与 4.3 的 EP 架构天然契合。**算法选择同时是系统选择**——这是 Co-Design 在特征层的最直接体现。

### 4.3 Embedding 架构：从 PS 到 EP + 三级存储

**结论先给出：Embedding 存放方式经历三代——PS（集中存放）、Dense/Sparse 混合（部分迁移）、EP + 三级存储（终态），驱动这条路径的是同一个矛盾：参数量涨到 TB 级后，所有 Worker 访问全部参数的通信开销压垂了系统，解法是让参数和计算的位置对齐。**

PS（Parameter Server）把 embedding 表集中存在专门节点，Worker 按需 pull/push。小模型时代（embedding 大、NN 小）合理，参数量上来后暴露两个问题：通信压力随参数量和 Worker 数超线性攀升，且无法和计算重叠；异步更新引入梯度 staleness 伤收敛，同步又退回带宽瓶颈。混合架构（Dense 走 DDP+NCCL，Sparse 留 PS）只是过渡——embedding 访问小粒度且随机，带宽利用率低，GPU 空等，搬运链路 PS→CPU 内存→网络→显存层层拷贝。

终态是 EP（Embedding Parallelism）：embedding 表按 ID 哈希拆分到各 GPU，通过 All-to-All 交换——batch 内 ID 去重、按哈希路由，All-to-All 分发查询、本地查表、All-to-All 回传，按 inverse_indices 复原顺序后 pooling。参数"分家"到各 GPU 点对点交换，带宽利用更高、延迟更稳。

再叠两级存储应对 ID 访问的长尾：Top-K 热 ID 走 DP 风格本地全复制，免掉 A2A 和跨机带宽；冷 ID 在 EP 分片查不到时向 CPU/SSD offload。热（本地复制）、温（EP 分片）、冷（CPU/SSD）三级，对应上 ID 分布的长尾特征。

### 4.4 数据传输路径：四级演进逐步绕开 CPU 瓶颈

**结论先给出：数据从存储到 GPU 显存的物理传输路径分四级演进，每一级解决上一级留下的瓶颈，核心方向是逐步绕开 CPU/内存这个中转站，让数据尽量直达显存。**

- **第一级，未优化**：HDFS→节点内存→DataLoader→H2D（host to device）。每次训练都要向远端 HDFS 发请求，瓶颈是网络吞吐和小文件读取延迟。
- **第二级，初级优化**：HDFS 一次大流量拉到本地 NVMe→顺序读入 pinned memory（GPU 可直接高效访问的内存区域）→non_blocking 异步拷贝与计算重叠。消掉了 batch 粒度的网络瓶颈，但 CPU/DRAM 仍是必经的中转站。
- **第三级，LLM 标配**：预切分 sharding，配合 GPUDirect Storage，数据直接从存储设备搬进显存，绕开 CPU/DRAM 中转。
- **第四级，顶配**：GPUDirect RDMA，数据从远端存储直达显存，全程零拷贝，本机 CPU 不参与。

瓶颈排查方法：直接在 GPU 上生成随机张量替代真实数据，若训练提速，说明瓶颈在 CPU 侧的 IO 读取路径；改成 CPU 生成数据再搬到 GPU，若还是慢，说明瓶颈在 H2D 拷贝或内存带宽；单卡快、多卡慢，则瓶颈在分布式通信。

### 4.5 对照 Scaling&Infra

本层与 Scaling&Infra 的交叉最密。主线 1（Token 化）在特征侧把异构信号统一成 token，本层在样本侧把 impression 组织成 user-wise——同一个"统一"原则在两个平面上的投影。主题 6（特征工程→Token 工程）的机理支撑在本文 4.2：记忆性特征的梯度竞争是"特征工程范式到期"的微观解释。Scaling&Infra 4.5 节"Sparse 终局是 Selective Shrinking"与本文 4.2 去记忆特征是同一判断的参数侧与特征侧两面——一个从 Scaling 曲线推（ID 覆盖度 × 样本新鲜度饱和），一个从训练动力学推（梯度抢占），结论同向。主题 3（近线计算）的推理侧对应物在本文 4.1：KV cache 持久化把"可以提前算的"进一步提前到"上一请求已算过"，其一致性债（缓存表征 vs 实时表征）与主题 3 的训练-推理一致性债同构。Scaling&Infra 主线 5 的存储与通信侧在本文 4.3/4.4 具体化到可实施。主题 4（Co-Design）则贯穿全层：样本组织决定架构可行性（user-level training 依赖 decoder-only），特征取舍决定 IO 带宽与 Embedding 架构选型。


## 5. 落地顺序

前四层不是并列关系，是有依赖的因果链——后一步的优化空间取决于前一步是否做到位，跳过前面直接做后面，效果会打折甚至无效。结合各层结论，一个推荐团队的改造顺序：

1. **过精度门槛**：换用 TF32/混合精度，或直接迁移到原生支持的框架，否则后面所有优化都无法触发 Tensor Core，一切免谈。验收看 TCA 与精度类型是否达标。
2. **维度对齐审计**：把矩阵形状凑到 16、128 的倍数，让 tile 能填满 SM。这一步不做，第一步换来的算力也用不满。验收看 tile 是否对齐、SM 利用率。
3. **主干统一成大维度 GEMM 结构**：d≥400 量级，加宽优先于加深，让算术强度过 Roofline 拐点。前两步保证了 Tensor Core 能被触发、tile 能填满，这一步才谈得上让计算真正进入 compute-bound。验收看静态 AI 与拐点的距离。
4. **显存换空间**：Flash Attention 必装，配合 DP+ZeRO-1/2，激活重计算按相变点权衡取舍。算术强度上去后，显存会成为下一个卡点，这一步给 batch 和序列长度腾出空间。验收看 OOM 边界、batch 与序列长度上限。
5. **样本组织改造**：从 request 内共享起步，批式重组是目标形态。显存打通之后，长序列才有意义，样本组织的收益才能兑现。
6. **特征减负**：记忆性特征按梯度竞争证据逐类下线，省出的带宽让前面几层的收益不被 IO 拖后腿。
7. **Embedding 架构升级**：EP + 热 ID 缓存 + 冷 ID offload，是前六层都打通后，针对 Embedding 这一特殊参数类型的收尾优化。

后三步（5-7）验收看同一个指标：GPU 数据空等占比是否下降。

## 附录 A：素材来源

- 系列总览：https://zhuanlan.zhihu.com/p/1943452214958982743
- GPU 利用率与 Tensor Core：https://zhuanlan.zhihu.com/p/1943453941414236260
- 计算强度与 Roofline：https://zhuanlan.zhihu.com/p/1943473025749070170
- 显存瓶颈：https://zhuanlan.zhihu.com/p/1943482521577853368
- 系统 IO 与样本范式：https://zhuanlan.zhihu.com/p/1943596579211674175

对照引用的论断以 `rec_model_scaling_infra.md` 为准；样本范式、去记忆特征、流式系统判断等属系列原作者的观点，本文在 5.4 标注了其与 Scaling&Infra 的差异处。

## 附录 B：数值口径备忘

- 利用率指标口径：GPU Utilization（整机）、SMA（SM）、TCU/TCA（Tensor Core）与 MFU（总账）分属不同主语、不同工具口径，不可互换引用；跨系统比较只认 MFU（见 1.1）。
- H100 峰值算力：本文统一采用 SXM FP16 dense 口径 989 TFLOPS、显存带宽 3.35 TB/s，Roofline 拐点 295 FLOPs/Byte 由此得出。系列原文表格中的 756/1513 为 PCIe 版口径、1979 为 SXM sparse 口径，跨表引用需换算。
- ZeRO 扩展效率（8 卡 99%/97%/90%，128 卡 95%/90%/75%）：公开文献与社区报告口径，量级参考，与论文实测不可并列引用。
- K=4095 对齐实测在 A100（108 SM）上取得；尾波计算在 H100 SXM（132 SM）上需按 SM 数换算。
- d≥400/375 的反解依赖"BL 远大于 d"的近似与静态 AI 假设；动态 AI 更低，实际下限更高。该阈值与 M=492（batch 反解）均沿用系列原文按拐点约 300 的近似反解；严格按本文 295 口径重解则略降（d≥394/369），结论方向不变，跨文引用时须注明所用拐点。
- §2.4 的两组 batch 反解口径不同：K=128/N=384 组的 AI 上限"96"含精度字节 b=2；K=1024/N=3072 组的 AI 上限"768"不含 b（沿用系列原文），若含 b 则为 384。两组结论（batch 18/95 与 M=492/batch≈5）本身不受此口径差异影响，但引用"AI 上限"数字时需注明是否含 b。
