# 推荐系统的算力兑现路径：从利用率到 IO 的四层硬件约束

> 推荐系统的架构判断不能脱离硬件单独谈——模型规模能放大到哪，取决于硬件兑现了多少；硬件的约束又反过来决定架构往哪收敛。这种双向牵制，正是 Scaling 从一开始就得和 Infra 工程侧一起 co-design 的原因。本文是 `rec_model_scaling_infra.md`（下称 Scaling&Infra）的另一维度补充。Scaling&Infra 自上而下：从 2025-2026 年 25 篇工业工作出发，整理出六层演进体系与七个统一主题，硬件指标在那里是分析工具。本文自下而上：从 GPU 的物理约束出发，经利用率、计算强度、显存、IO 四层逐级向上推到样本组织，回答同一个问题的另一半——**硬件为什么只奖励那些做法**。
>
> 素材声明：本文的硬件层素材主要来自作者平时的文献阅读、工作积累与深入思考（原始版本发布于知乎"推荐系统的算力突围"系列，一篇总览 + 四篇正文，链接见附录 A），包含一手工程实测与线上 AUC 经验；对照部分引用的论断以 Scaling&Infra 为准，数值口径注意事项统一列在附录 B，跨文档引用数字前请先对齐口径。

## 0. 硬件约束是一条因果链，四层缺一不可

Scaling&Infra 主题 1 给出过结论：所有优化的根源是 GPU 没被喂饱，碎片化的小算子让 MFU 只有个位数。这个结论自上而下看是 25 篇论文的共同规律，自下而上则是四层串联的因果链：

```text
范式 GEMM 化（吃到 Tensor Core）
  → 提升算术强度，跨过 Roofline 拐点（进入 compute-bound）
    → 优化显存（给 batch 和序列腾出空间）
      → 优化 IO（让 GPU 不空等数据）
```

四层是串联的：任何一层不通，堆卡只是增加空转成本。业界的普遍状态是卡在最前面——模型范式还是"小卡遗产"（Embedding 查表 + 短序列 + 小矩阵 MLP），算术强度普遍低于 10 FLOPs/Byte，处在 memory-bound 最左端。觉得卡数不够的系统，应先核查 Tensor Core 口径的利用率（总体 TCA / MFU），而不是看板上的 GPU Utilization——那个数字恰恰最该忽略（见 1.1）：多数情况不是算力不足，而是算力从未被兑现。

这条因果链不是理论推演，LLM 已完整走过一遍：从主算子 AI 仅几十到一百、多数训练 memory-bound，到主循环 compute-bound、吞吐随 Tensor Core 峰值缩放，靠的正是范式 GEMM 化、算子与精度改造、显存与 IO 工程的逐层打通（提升史见 2.3）。推荐系统与 LLM 的差距不在卡，在四层中每一层的完成度。

这四层给出的不只是一套硬件知识，更是一套可以直接拿来判断"某个模型设计能不能兑现算力"的依据，也是本文能跟 Scaling&Infra 逐条对上号的原因：后者说"接口要统一成一个交互 Block"，对应的是第一层范式 GEMM 化能不能吃到 Tensor Core；说"长序列这条路走得通"，对应的是第三层显存能不能跟上 batch 和序列长度；说"特征工程这条老路快到头了"，对应的是第四层 IO 跟不上时，堆多的特征会挤占带宽、抢占梯度。矩阵维度够不够（对应第二层算术强度能不能过 Roofline 拐点）、序列长了会不会先被显存挤死、特征堆多了梯度会不会被抢占，都能在方案定稿前算出来，不用等训练跑起来、显存爆了或者吞吐上不去才回头改设计——省下来的是一轮返工的时间和一批卡的空转成本。第 5 节给出逐条对照。

## 1. 第一层：利用率的正确读法

### 1.1 利用率指标：只有 Tensor Core 口径能跨系统比较

性能看板上最常见的 GPU Utilization，统计的是采样周期内"至少有一个 SM 在执行指令"的时间占比——这个指标粒度很粗：只要芯片上有任何一小块在动，就算在忙。所以 GPU 利用率 80% 以上，完全可能对应计算引擎大量空闲。

往下一层看 SM 内部，是 SM Activity（SMA）：至少一个线程在发射指令就算活跃，哪怕这个线程只在做 Load/Store（搬数据，不做乘加运算），SM 也计满。所以 SMA 100% 时，算力发挥可能不到 10%——忙的是数据搬运，不是计算。

再往下看 SM 里真正做乘加运算的部件，才是 Tensor Core。它有两个口径：TCU（Tensor Core Utilization）看时间，即采样周期内 Tensor Core 工作的时间占比；TCA（Tensor Core Active）看空间，即执行指令时实际活跃的运算单元比例。两者会一高一低：TCU 高 / TCA 低，说明算子占着时间长但矩阵小、分块不合理；TCU 低 / TCA 高，说明单次执行满载但被非 Tensor Core 算子挤占了时间。系统报表里的"总体 TCA"是时间加权值，约等于 TCU × TCA。

这三层（整机→SM→Tensor Core）看的是同一块芯片，但粒度越来越细，数字也越来越"不好看"。原因是硬件本身的算力就高度集中在 Tensor Core 上：H100 上 Tensor Core 的半精度算力约为 CUDA Core FP32 算力的 15 倍，总算力的九成以上都在这里。所以"GPU 算力兑现了多少"这个问题，本质上问的是 Tensor Core 忙不忙，而不是整机忙不忙——GPU Utilization 只回答"芯片上有没有东西在跑"，从不回答"跑的是不是真正在算数的那部分"。这也是为什么比较不同系统的算力兑现水平时，只能认 Tensor Core 口径，GPU Utilization 和 SMA 数字再高都不能拿来比。

MFU 是唯一可以跨系统比较的总账指标：实际 FLOPs 除以硬件理论峰值。它与其他指标的关系是：

```text
MFU ≈ 总体 TCA × η    （η ∈ (0,1]，由算术强度不足与流水线空洞决定）
```

一个类比：TCA 是"多少台机器有人上岗"，η 是"上岗的机器有多少时间在加工"。原料供应不足（算术强度低）或流水线卡顿（pipeline 空洞）时，机器都站在那里，产出却很少。这也解释了为什么总体 TCA 高的同时 MFU 可以很低——两者之间隔着一个 η，而 η 正是第二层 Roofline 的主角。

### 1.2 精度门槛：不满足就无法触发 Tensor Core

Tensor Core 不是默认开启的。它要求特定的数据精度类型，而大量推荐系统仍在 TensorFlow 1.x 上运行——不支持 TF32，未做混合精度改造时矩阵运算全部由 CUDA Core 以 FP32 执行，TCA 为 0。以 H100 为例，FP32 的 CUDA Core 算力约 67 TFLOPS，Tensor Core 半精度约 989 TFLOPS（SXM dense 口径），相差约 15 倍。TCA 为 0 意味着第一层的全部优化无从谈起。存量系统的第一步是让计算图跑在支持 TF32/FP16/BF16 的框架和精度下——可以是升级到 TF2.x 并开启 TF32，可以做混合精度改造使用 FP16/BF16，也可以直接迁移到原生支持这些精度的 PyTorch，三条路殊途同归：让矩阵运算落到 Tensor Core 而不是 CUDA Core。这是一次性的门槛成本，也是后续所有优化的前提。
<img width="800" height="480" alt="image" src="https://github.com/user-attachments/assets/7f1cd3ac-b34d-4dd8-8826-8c80d105d981" />


### 1.3 维度对齐：为什么矩阵形状要凑 16、128 的倍数

维度规则不是经验口诀，而是从 GEMM 在硬件上的执行模型推出来的。执行结构分四级：SM 是 GPU 的核心，内含寄存器文件、共享内存、调度器与 Tensor Core；Warp 是 32 线程的最小调度粒度，同一 Warp 同时执行同一条指令；Block 运行在单个 SM 上（一个 SM 可同时跑多个 Block），Block 内的 Warp 经共享内存协作；Tensor Core 是 SM 内的矩阵运算单元，被 Warp 调用。一个大 GEMM C[M,N]=A[M,K]×B[K,N] 会被逐级拆解：输出矩阵先切成 Block tile（常为 128×128 或 256×128），每个由一个 Block 独立完成；Block tile 内再切 Warp tile（如 64×64 或 32×64）；Warp 内由 Tensor Core 指令完成 micro-tile 乘加，线程各自持有寄存器 fragment，累加少量元素后合并成完整输出。

<img width="800" height="480" alt="image" src="https://github.com/user-attachments/assets/c5728197-4524-43c7-9229-7b7129bf98de" />

与执行结构并排的是存储层次：寄存器线程私有、访问最快但容量小，寄存器不足时数据溢出到更慢的层次，直接拖累算力；共享内存与 L1 位于 SM 内、由 Block 内线程协作使用，是 tile 数据复用的主战场；L2 由全体 SM 共享，充当全局缓冲；HBM 显存容量与带宽最大，延迟也最高。tile 数据的装载路径固定：Block 级 tile 从显存读进共享内存，Warp 级 tile 从共享内存读进寄存器，寄存器 fragment 参与 Tensor Core 运算。计算单元决定理论算力上限，存储层次决定这个上限能否被触发——大规模 Embedding Lookup 难以在片上复用数据、只能反复访问显存，正是推荐系统 memory-bound 的微观形态。下面三条规则，全部由这套拆解与装载机制导出。


过了精度门槛，矩阵维度决定 Tensor Core 能否高效执行。三条规则各有实测：

**维度须为 16 的倍数。** 半精度 MMA 的最小计算单元是 16×16×16，三个维度对应 M/N/K 三个方向的 micro-tile 切片；这条规则约束的是 K：K 不被 16 整除时，Warp 内沿 K 方向做 micro-tile 乘加会因为凑不满一个 16 的切片而 padding，浪费的是指令级算力，与 M/N 方向的 tile 切分是两回事。实测 A100 上 K=4095 对比 K=4096：TF32 慢 64%，FP16 慢 190%，BF16 慢 197%——只差一个元素，耗时翻倍。

**M/N 取 128 或 256 的倍数。** 计算库的 thread block tile 通常为 128×128 或 256×128，访存事务按 256B 对齐（对应 128 个 FP16 元素），不满块就 padding。tile 量化浪费可以算出来：100×100 的 QK^T 在 128×128 tile 下浪费约 39%，且与 B、H 无关——同一份浪费被重复执行 B×H 次。

**tile 总数尽量是 SM 数的倍数。** SM 数量（A100 为 108，H100 SXM 为 132）决定一轮并行能装多少 tile。这条规则约束的是 M/N：256×128 的 Block tile 只切分 M、N 两个方向，tile 总数由 M、N 决定，K 不参与这次切分——K=4096 在这里只是背景参数，决定的是每个 Block 内沿 K 方向要做多少轮 micro-tile 累加，不影响 tile 数量。M=2304、K=4096、按 256×128 分块时，M 方向 9 个 tile，108 个 SM 一轮恰装 12 列，对应 N=1536——N 每突破 1536 的倍数，tile 数只多一点，执行时间却翻倍。这就是尾波（tail wave）：107 个 SM 陪 1 个 SM 跑完最后一轮。

落到 Transformer 的推荐改造上，实操清单是：序列长度 L 取 128 的倍数（条件不允许时至少 16 的倍数）、head_dim 取 64 或 128、model_dim 取 128 的倍数、batch size 在矩阵小时精算 tile 数是否凑满 SM。

### 1.4 对照 Scaling&Infra

主题 1 的 MFU 表（RankMixer 4.5%→45%、Kunlun 17%→37%、OneRec 极低→23.7%/28.8%）在这一层有了机制解释：碎片化小算子同时压低 TCA（矩阵小、tile 不满）和 η（算术强度低、kernel 启动开销占比高），统一成大 GEMM 是同时修复两者。4.4 节提出的"GEMM 统一度"指标，其硬件微观基础就是本节的 tile 与尾波分析——统一度高意味着维度整齐的大矩阵：M/N 够大能切出足够 tile 填满 SM，K 够深则同一份从显存读入共享内存的 Block tile，能在 K 方向被更多轮 micro-tile 乘加反复复用（该复用机制的算术强度推导见 2.1）。4.4 的"硬件对齐分层筛选"（先筛 8/16 对齐，再做离散消融）落地到本节就是那份实操清单。

## 2. 第二层：Roofline 与计算强度

### 2.1 Roofline 公式：算力和带宽谁是瓶颈

Roofline 模型把计算能力与存储带宽的关系压进一条公式：

```text
Performance = min(Peak FLOPs/s, AI × Memory Bandwidth)
```

AI（Arithmetic Intensity，算术强度）= 计算量 / 访存量，单位 FLOPs/Byte。AI 低于拐点时性能被带宽卡住（memory-bound），高于拐点时被算力卡住（compute-bound）。H100 SXM 的拐点约 295 FLOPs/Byte（989 TFLOPS ÷ 3.35 TB/s）。推荐系统的普遍状态是 AI 低于 10，处在最左端——这意味着买了 H100 的算力，实际用到的只有它的显存带宽。

对 GEMM（A[M,K]×B[K,N]→C[M,N]，精度字节 b）：

```text
AI ≈ 2MNK / b(MK + KN + MN)
```

分子是三维乘积（立方增长），分母是两两乘积（平方增长）。三维等比放大 s 倍，AI 线性增长 s 倍；只放大一维，AI 收敛到与其他两维相关的常数。一句话：矩阵要够胖（方）。硬件对应：M/N 大且相近，tile 切得多、缓存复用充分；K 深，16×16×16 的 micro-tile 够多，一次全局加载被反复榨取。

AI 分静态与动态：静态 AI 假设中间结果全部驻留寄存器/共享内存、缓存命中 100%，是理论上限；动态 AI 按运行时性能计数器的真实访存量计算，包含中间结果写回、缓存未命中、padding 浪费。模型设计决定静态 AI，算子融合与内核优化让动态 AI 逼近静态 AI。这一区分贯穿本层和第 4 层的分析。

### 2.2 提升算术强度：加宽比加深更有效

加宽（增大 d_model、head_dim、FFN 中间层）同时提升 FLOPs 和 AI；加深（堆更多层）只提升 FLOPs，单层 AI 不变。在 memory-bound 区间，加深的算力增长被同样的访存增长抵消——**加深在 memory-bound 区是无效投资，这是反直觉但严格成立的结论**。

但加宽优先不等于深度无用。LLM 的 compute-optimal 研究（Chinchilla）给出近似最优宽深比 d_model∝P^0.4、n_layers∝P^0.3：深度仍贡献建模能力与表达深度，只是在 memory-bound 区其算力增长被访存增长同步抵消；跨过拐点后，加深恢复为有效投资。最优宽深比依赖特征数量、embedding 尺寸、序列长度与任务目标权重，推荐系统需要像 LLM 一样系统性探索自己的宽深边界，而不是套用单边结论。

用 Transformer Base 的两个大算子算具体数字（b=2，BL 为 batch×序列长度）：

- QKV GEMM（[BL,d]×[d,3d]）：BL 远大于 d 时 AI ≈ 1.5·d/b，随 d 线性增长；d 远大于 BL 时饱和于 2·BL/b
- FFN 单次 GEMM（[BL,d]×[d,4d]）：BL 远大于 d 时 AI ≈ 1.6·d/b

对 H100 的 295 拐点反解：QKV 需要 d ≥ 400，FFN 需要 d ≥ 375。**推荐系统常用的 128/256 隐层维度，无论怎么加 batch 和序列长度，AI 都够不到 295**——小维度下 M 维增长收敛到的常数上限太低。这是"统一交互 Block 应该做多大"的第一个硬数字下限，也是对主线 2 的直接支撑：RankMixer 路线把主干做成大维度 MLP-Mixer，不只是结构上的统一，是过拐点的必要条件。

算术强度还有一层算法侧解读：AI powers AI。更高 AI 的结构，意味着相同访存信息下计算的连接与交互更密集，更能产生智能——Scaling Law 研究中，无论扩参数还是扩 token，高 AI 的 Transformer 都比低 AI 的 LSTM 有更好的 Scaling 曲线。推荐系统持续借鉴 NLP 成果，根源在于用户行为序列与文本 token 序列的定义相似：拉长序列不只是线性提升 AI，也在扩宽可挖掘的知识面。AI 因此不只是省钱指标，也是度量算法潜力能走多远的标尺。

### 2.3 QK^T 的 L² 陷阱与 Flash Attention 的战略地位

Attention 的 QK^T 不是一个大 GEMM，而是 B×H 个并行的 [L,d_h]×[d_h,L] 小 GEMM：

```text
AI(QK^T) = 2L²d_h / b(2L·d_h + L²)
```

它只依赖 L 和 d_h，与 B、H 无关。L 很大时 AI 极限是 2d_h/b——一个与 L 无关的常数。平方级的算力增长被平方级的访存增长抵消：**不做 Flash Attention 之前，拉长序列的 Scaling 在数学上走不通**，无论特征序列还是行为序列。

Flash Attention 利用 Softmax 平移不变性，把 QK^T、Softmax、乘 V 整体搬到片上 SRAM 分块完成，L×L 注意力矩阵从不落显存，访存从 O(L²) 降到 O(L)：

```text
AI(QK^T | FA) ≈ L/b    （随序列长度线性增长）
```

这给 Scaling&Infra 主线 3 的判断提供了独立推导：Flash Attention 的战略意义不是加速比，而是解锁"拉长序列"这条 Scaling 维度——它是可行性前提，不是锦上添花。LLM 的算术强度提升史走的是同一条路：GPT-2/3 时代主算子 AI 仅几十到一百 FLOPs/Byte、多数训练 memory-bound；GPT-3.5/PaLM 时代扩 batch、拉长 L、FA v1/v2 出现；GPT-4/LLaMA3 时代长上下文加巨批次，主循环已 compute-bound，吞吐随 Tensor Core 峰值缩放。

### 2.4 Batch Size 的作用被高估：真实门槛在维度，不在 batch

"加 batch 提吞吐"是推荐系统的经验，但用 QKV 的 AI 公式检验，它是 memory-bound 区间的特例。K=128、N=384、L=100 的典型推荐设置下，M 趋于无穷的 AI 上限是 3K/4 = 96，进不了 compute-bound；取极限的 95% 和 99% 反解得 M 分别为 1824 和 9504，除以 L=100 折成 batch 分别是 18 和 95——batch 从 18 加到 95，理论吞吐只涨 4%，且大概率先被显存卡死。换 K=1024、N=3072：M 趋于无穷时 AI 上限按 KN/(K+N)（不含精度字节 b）算是 768，这是系列原文的口径，与前一算例含 b 的静态 AI 公式不统一，此处沿用原文数字，下文按含 b=2 的静态 AI 反解；解 AI=300 得 M=492 过拐点（K=128 案例的 295 拐点在此处约为 M=479，结论不受影响），M/L≈5，即 batch=5 之后继续加大 batch 吞吐不再增加。

M 只是把 GEMM 推向该形状（N、K 确定）的 AI 极限：隐层维度小时极限低，怎么加 M 都进不了 compute-bound，但在 memory-bound 区间 AI 与吞吐随 M 增长（收益快速递减）；隐层维度够大时存在有限门槛，过了就不再涨。这也解释了 LLM 为什么敢用 Gradient Accumulation：d 够大时 micro-batch 很小就到达吞吐极限，拆分 batch 不掉吞吐。

### 2.5 算子融合：减少读写次数，逼近理论算术强度

静态与动态 AI 的差距由三件事拉开：中间结果写回再读出、缓存未命中、kernel 启动与调度开销。Fusion 一次解决三件：中间结果留在寄存器/共享内存；一次访存多次计算；N 个 kernel 的启动延迟摊成一个。QKV 三个独立 GEMM 对比融合为 [d,3d] 单次 GEMM：X 读取 3 次→1 次，访存 3.0x→1.2-1.3x，动态 AI 1.0x→2.5-3.0x。kernel 启动延迟在几到几十微秒量级，对小计算量算子（激活、归一化）可占总执行时间的 30-50%；连续短 kernel 还会造成流水线泄露——pipeline 未装满即结束，吞吐率明显下降。

启动开销还有融合之外的互补解法：CUDA Graphs。它不改访存与计算，把整段 kernel 调用图一次性交给 GPU 按序批量执行，CPU 启动次数从 N 次降为 1 次，消掉 CPU→GPU 往返延迟与调度空转；kernel 小、调用频繁的推荐场景收益尤其明显，可与 Fusion 叠加使用。

融合分三级：编译器自动融合（XLA / TorchInductor，透明但不可控）、显式融合算子（Flash Attention 这类，可控但维护成本高，优先复用 LLM 生态的开源算子）、写法级触发融合（将 `gelu(x @ w + b)` 保持在一行让编译器识别）。推荐系统侧的具体提醒：任何 concat 都应审视是否需要融合——concat 本身零计算、AI 极低，却产生一次显存写和读；XLA 不保证自动融合 concat 与后续 GEMM，稳妥做法是用 batched matmul 在一个 kernel 内完成多 GEMM 相加（K 维均为 16 倍数、M×N 够大时，性能与大 GEMM 几乎无差）。side-info 的 embedding 维度也应取 16 的倍数：为省存储选 4/8 维，padding 损失反而更大。

side-info 与 ID 的融合方式选型，是算法-系统 Co-Design 在特征层的完整案例。三种方案：其一，side-info 与 ID 取同维 embedding、逐元素相加；其二，各自按需选维度、独立线性映射到同维再相加；其三，按信息容量选维度、concat 后线性映射到 token 维度。其二与三实现不同，考虑 fusion 后殊途同归，即前述 batched matmul 的一次 kernel 多 GEMM 相加。系统侧权衡在存储与带宽：其二/三按信息量入为出，embedding 存储省；其一可以在 GPU 外（PS 侧或 EP 本地）先做 sum pooling，省掉网络带宽与 GPU 的多次 GEMM——side-info 表更稠密、行数远少于 ID 数，同维浪费的存储并不大。瓶颈在带宽还是存储，决定选哪边。

算法侧还有一个容易被忽视的收益：语义锚点。系列原文的表述是"同维相加后 embedding 接收的梯度是 1/m"（m 为相加项数）——严格按 sum pooling 求导，每一路的梯度系数应为 1 而非 1/m，1/m 更准确对应 mean pooling，此处沿用原文说法，核心论点不受这处口径影响：batch 里稀疏 ID embedding 随机初始化的权重本会产生近乎随机的梯度，却跟着稠密的类目/品牌 embedding 走了一步——稀疏侧被稠密侧拉着走，相当于隐式正则：类目/品牌 embedding 把相似 ID 拉近，冷启动与长尾 item 学得更好。其二方案的 Linear 变换能学习 side-info 重要性，算表达能力上的弥补；两种路线表达能力不相上下，选型可让系统瓶颈做主。

### 2.6 对照 Scaling&Infra

主题 1 的"成本算术题"在这里有了定量版本：MFU 4.5% 不是"模型差"，是 AI 低于拐点时性能被带宽钉死，95.5% 的 Tensor Core 在空转。RankMixer"百倍参数、推理延迟不变"是把新增 FLOPs 装进空闲窗口，但这个解释只对 serving 口径成立：推理小 batch 下的时间大头是碎片算子的启动与访存开销，大 GEMM 化既压缩开销又装进新计算，延迟才可能持平；若拿训练 MFU 4.5%→45% 验算（时间不变只能容纳约 10 倍 FLOPs），账就对不上——训练与推理是两本时间账，不能互相验算。Scaling&Infra 4.7.1 对压缩后 Self-Attention 的两种口径分析（非融合实现 AI≈d，融合实现 AI≈O(L) 且与 d 基本无关）与本文 2.3 的推导同构——Scaling&Infra 用它反推压缩比下界，本文给出该推导的源头，两份文档在这条方法上互相验证。Scaling&Infra 4.6 的 gather-GEMM 双缓冲 staging（把随机访存变成异步预取加流水掩盖）与本文 2.5 的 Fusion 同源：都是压缩动态 AI 与静态 AI 的差距，只是对象从"算子间中间结果"换成"gather 输出"。

## 3. 第三层：显存

### 3.1 显存分四块，激活值是占比最大的一块

训练显存四块：参数（1B 参数 FP32 为 4GB，混合精度为 6GB——FP32 主权重 4GB 加 FP16 计算副本 2GB）、梯度（1B 参数混合精度 2GB）、优化器状态（Adam 每参数一阶加二阶动量，1B 参数 8GB，且须 FP32——动量是梯度的指数滑动平均，训练初期梯度小，半精度存储会精度下溢，更新趋近于零）、激活值。第四块是大头也是复杂度所在：反向传播需要中间结果，B=1024、H=16、L=1024、FP16 下单层 Attention Map 即 32GB；另一组常见配置（B=512、L=1024、H=8、d=512、4 层）下 Attention Map 32GB、QKV 投影约 6GB、FFN 激活约 18GB，三项均为 4 层合计，激活总量约 60GB。1B 模型在 80GB 卡上 OOM，大概率是激活而非参数。

### 3.2 激活值优化：三种手段各解决哪种显存压力

四种手段各治各的病：Checkpointing 以算换存（分段保存、反向重算，显存从与总层数成正比降到与段长成正比，适合 memory-bound 区——计算资源比显存充裕时划算）；Gradient Accumulation 保收敛不提 AI（显存受限时调低 batch 换大 d 可提升矩阵方形程度与 AI，但小 batch 梯度方差增大、更新不稳定伤收敛；GA 把大 batch 拆成 micro-batch、累计到 effective batch 再更新，解除这个两难——2.4 的结论说明小维度下这是双输，大 d 下无损）；Flash Attention 把激活从 O(L²) 降到 O(L)（在线 Softmax 只保留每 query 的分子分母累积量，数值上用平移不变性做跨块缩放）；混合精度把激活减半。

Flash Attention 的在线 Softmax 值得单独展开，它是 2.3 里长序列可行性前提的数学内核。K、V 分块装进片上 SRAM，每块在片上完成 Softmax 与乘 V，全程只维护每 query 的两个累积量：分母 l_i（指数和）与分子 o_i（加权和），两者均与 L 线性。跨块之所以能直接累加，靠的是平移不变性：e^s 在 FP16/BF16 下指数超过约 11 即溢出，Softmax 必须减去最大值 m 再取指数；分块时无法预知全局最大值，就在线维护当前块最大值，把已累积的 o_i、l_i 乘 e^(m_old-m_new) 统一到新基准。缩放链连乘后恰好抵消，等价于全部减去最终的全局最大值——分块结果因此可以直接相加，L×L 注意力矩阵从头到尾不落显存。

这里有一个值得写清的点：BF16 指数位与 FP32 相同、几乎不溢出，为什么仍要 FP32 主权重？BF16 尾数只有 7 位（约 2 位十进制有效数字），FP32 有 23 位（约 7 位）。优化器更新量由学习率与梯度量级决定，二者都小时，更新量低于 BF16 的表示精度，被截断成零——权重不再移动。主流框架在 BF16 模式下仍保留 FP32 master weights，推理阶段才可直接用 BF16 权重。

数值稳定的另一处在 loss：FP16 指数位仅 5 位、动态范围有限，小梯度会下溢，须对 loss 放大再反向传播；BF16 动态范围与 FP32 一致，免去 loss scaling——这是混合精度改造优选 BF16 的又一个直接原因。

### 3.3 并行策略选型：推荐系统为什么优先选数据并行

DP/TP/PP 三种并行里，TP 切单层、通信频繁，PP 按层切、有流水空泡，两者的复杂度收益比在"参数不大"的推荐模型上都不划算。判断标准很直接：总参数 3B 以内且单卡 80GB 能跑通，就用 DP + ZeRO-1/2，128 卡内吞吐损失可控制在 10% 内（ZeRO 各 stage 扩展效率为公开文献与社区报告口径，供量级参考，见附录 B）。ZeRO 对优化器状态、梯度、参数逐级分 shard，用 All-Gather / All-Reduce 在需要时组装，用完即释放。优化器侧的备选（AdaFactor 状态从 O(n) 降到 O(√n)、Lion/Sophia 只留一阶动量、优化器状态 offload 到 CPU）在推荐场景多数用不上——参数与状态占用本来不大，选型应以效果为先。

### 3.4 对照 Scaling&Infra

主线 5（底层基建）的训练侧在本节具体化。更重要的是主题 1 的相变点在这一层有一个代价反转：Checkpointing"以算换存"在 memory-bound 区划算（计算闲着），跨入 compute-bound 后重算成本变成真实算力开销，ULTRA-HSTU 的选择性激活重计算（只重算便宜的、不重算贵的，单层显存 7GB→2.3GB）正是在这条边界上做精细权衡。显存层与第二层是耦合优化：腾出的显存换更大的 batch 和 L，而 2.4 说明只有 d 够大时这笔交换才有效——单维优化显存不会自动带来吞吐。

## 4. 第四层：IO 与样本范式

前一层打通后，GPU 仍可能长期空等——数据供给跟不上计算节奏。推荐系统尤其严重：样本量庞大、特征稀疏、Embedding 参数 TB 级，数据加载、特征访问、参数拉取都是带宽消耗点。IO 优化的三个目标是减少传输总量、缩短传输路径、提升带宽利用率。这一层的关键判断是：最有效的 IO 优化不在传输层，而在样本与特征的范式层。

这个判断有一个范式级的诊断前提。深度学习进入推荐时，把 LR 的一维 weight 换成 embedding 塞进 MLP，各家总体提升并不大，基线的手工特征越强、增益越难体现；真正带来显著增益的是用户序列建模——行为序列是传统方法难以处理的特征形式，深度架构能有效捕捉其中模式。此后排序模型固定为标准范式：序列建模先产出 pooling 后的表征，再与其他特征一起进特征交叉结构，即 MLP/DCN(concat(DIN(序列), 其他特征))；把 DIN 换成 Transformer、把 MLP 换成 TokenMixer，复杂化的是结构，范式从未变化。范式内替换已到边际，用户序列的知识挖掘又仍处早期，突破口于是落在样本组织与数据通路上——这是 4.1 到 4.5 的立论起点。

### 4.1 point-wise 样本的平方级冗余

病根在样本组织：point-wise 模式下每个 impression 一条样本，一个 request 内几十个 impression 的用户侧特征完全相同且重复存储；序列拉长到千级后，一天内的样本绝大部分历史行为互相重复。这是 impression × 历史 engagement 的平方级冗余，存储和网络 IO 双重浪费。序列越长，纯用户侧（item-unaware）特征在样本里的重复占比越高，冗余越严重——长序列 Scaling 与 point-wise 样本在结构上不兼容。

### 4.2 样本重组三级跳

重组路径三级：

- **第一步，request 内共享序列**：impression 共享一条用户历史序列，改造代价小，batch 与流式训练都兼容。
- **第二步，M 天聚合的 pre-train Sample**：把用户 M 天内的 request 聚合成一条样本，用 index 指针挂在用户长序列上。行为丰富用户做多条切分，行为稀疏用户拉长时间窗口，多个 request 共享同一份历史。M=30 即用户一个月的样本全部合并——以往全量重训成本巨大、只能部分加载权重 warm start，这份压缩样本大幅降低历史全量重训的成本。
- **第三步，N 天切分的 incremental-train samples**：为兼容 one-epoch 增量训练，把 M 天内样本按 N 天切分（N=1 即 daily 增量），N 天内 request 共享历史序列。

一个有分量的判断：流式训练系统最多改造成 request 级共享，绝大多数重复序列计算无法避免——这类系统在算力驱动的 Scaling Path 上会持续落后。批式样本重组不是工程偏好，是长序列路线的前提（该判断的适用边界见 5.4）。

### 4.3 user-level training 与推理侧 KV cache

样本重组只省了存储和 IO，重复计算要靠模型结构配合。decoder-only 的因果注意力使远期行为的 Key/Value 不受近期行为影响，近期行为作为 Query 查询远期 KV——于是用户序列只计算一遍，target 以自身 ID 和 side-info 为 Query 与历史序列做 Attention。推理侧同构：模型泛化性够时一段时间不更新，用户 Session 内甚至 N 天内的历史 KV 可持久化，新行为只更新增量 KV，请求到来时只做 target 与历史 KV 的交互。工程上选 Session Affinity（同一 UID 打到同一 GPU 节点，KV 缓存按显存/内存/SSD 分级存放）而非分布式 KV 存储——实现简单、延迟最低，省掉序列化与网络传输开销。

### 4.4 去记忆性特征：既省带宽，也让梯度不被稀疏特征抢占

记忆性特征（稀疏笛卡尔积交叉、UID 大规模 FFM 交叉、用户 Match 统计特征）是存量基线里的强特征，收敛快。但它们在训练初期产生更强的梯度信号，抢占有限的学习能力，压制泛化结构的训练——one-epoch 场景尤甚：记忆性部分先行收敛、loss 快速下降，泛化结构学习不足，模型过度依赖历史数据，冷启动与长尾无解。原作者的一手经验：在复杂模型上加入这些特征，长期 AUC 下降；目前已去掉笛卡尔积与 Match 特征，下一步动 UID 隐式交叉。

另一面在 IO：这些特征占满数据通路——序列拉长后通路的增量几乎全部来自 item 序列特征，记忆性特征不带增量却持续占着带宽，砍掉它们同时给数据通路减负；去掉之后最大的参数块收敛到物品 ID embedding，与 4.5 的 EP 架构天然契合。**算法选择同时是系统选择**——这是 Co-Design 在特征层的最直接体现。

### 4.5 Embedding 架构：从 PS 到 EP + 三级存储

PS 是小模型时代（海量 embedding + 小 NN）的合理选择，参数量上来后暴露两个问题：带宽与延迟（所有 Worker 向 PS push/pull，压力随参数量与 Worker 数线性甚至超线性攀升，通信路径长、无法与计算重叠；异步更新引入梯度 staleness 伤收敛，同步又回到瓶颈）；混合架构（Dense 走 DDP+NCCL、Sparse 留 PS）只是过渡——小粒度随机 pull/push 让网络带宽利用率很低，GPU 空等 embedding 返回，搬运链路 PS→CPU 内存→网络→显存层层拷贝。

终态判断是 All-to-All 的 EP（Embedding Parallelism）：batch 内 ID 去重后按哈希路由到持有该行的 GPU，All-to-All 分发、本地查表、All-to-All 回传、按 inverse_indices 复原顺序后 pooling。EP 把参数"分家"到各 GPU 点对点交换，带宽利用更高、延迟更稳。再叠两级：Top-K 热 ID 走 DP 风格本地缓存（与 Dense 一样全复制，免掉 A2A 与跨机带宽）；冷 ID 在 EP shard 查不到时向 CPU/SSD offload。热温冷三级存储，与推荐 ID 分布的显著长尾正好对上。

### 4.6 数据传输路径：四级演进逐步绕开 CPU 瓶颈

数据通路从裸到顶配四级：HDFS→节点内存→DataLoader→H2D（未优化，HDFS 网络吞吐与小文件延迟是瓶颈）；HDFS 一次大流量拉到本地 NVMe→顺序读入 pinned memory→non_blocking 异步拷贝与计算重叠（初级优化，消掉 batch 粒度网络瓶颈，但 CPU/DRAM 仍是中转）；预切分 sharding 加 GPUDirect Storage 直达显存（LLM 标配，绕过 CPU DRAM）；GPUDirect RDMA 从远端存储直达显存（顶配，全面零拷贝）。瓶颈排查的方法同样值得保留：直接在 GPU 上生成随机张量，若训练提速则是 CPU IO 路径问题；CPU 生成再搬 GPU 若慢则是 H2D 或内存带宽问题；单卡快多卡慢则是分布式通信问题。

### 4.7 对照 Scaling&Infra

本层与 Scaling&Infra 的交叉最密。主线 1（Token 化）在特征侧把异构信号统一成 token，本层在样本侧把 impression 组织成 user-wise——同一个"统一"原则在两个平面上的投影。主题 6（特征工程→Token 工程）的机理支撑在本文 4.4：记忆性特征的梯度竞争是"特征工程范式到期"的微观解释。Scaling&Infra 4.5 节"Sparse 终局是 Selective Shrinking"与本文 4.4 去记忆特征是同一判断的参数侧与特征侧两面——一个从 Scaling 曲线推（ID 覆盖度 × 样本新鲜度饱和），一个从训练动力学推（梯度抢占），结论同向。主题 3（近线计算）的推理侧对应物在本文 4.3：KV cache 持久化把"可以提前算的"进一步提前到"上一请求已算过"，其一致性债（缓存表征 vs 实时表征）与主题 3 的训练-推理一致性债同构。Scaling&Infra 主线 5 的存储与通信侧在本文 4.5/4.6 具体化到可实施。主题 4（Co-Design）则贯穿全层：样本组织决定架构可行性（user-level training 依赖 decoder-only），特征取舍决定 IO 带宽与 Embedding 架构选型。

## 5. 合流：验证、补充与张力

### 5.1 逐条对照表

| Scaling&Infra 论断 | 本文对应 | 关系 |
| --- | --- | --- |
| 主题1：MFU 是一切优化根因；成本算术题 | §1.1 MFU≈总体TCA×η；§2.1 拐点 295 | 机制解释与定量化 |
| 主题1：memory→compute 相变点 | §2.4 batch 特例；§3.4 checkpointing 代价反转 | 边界条件补充 |
| 主题2：架构前提（统一大 GEMM） | §2.2 d≥400/375 反解 | 给出硬下限 |
| 主题2：数据前提（时间窗甜点） | §4.2 pre-train Sample | 张力（见 5.4） |
| 主题2：表征前提 | 该系列未覆盖 | 缺口，仍以 Scaling&Infra 主题 7 为准 |
| 主题3：近线计算与三笔债 | §4.3 KV cache / Session Affinity | 推理侧对应物 |
| 主题4：Co-Design | §4 全层 | 最系统的实证 |
| 主题6：特征工程→Token 工程 | §4.4 梯度竞争 | 机理支撑 |
| 主线2：统一交互 Block | §2.2 加宽 vs 加深 | 必要条件证明 |
| 主线3：长序列压缩；FA 战略意义 | §2.3 L² 陷阱与 FA 后 AI≈L/b | 独立推导互相验证 |
| Scaling&Infra 4.4：GEMM 统一度与分层筛选 | 本文 §1.3 tile/尾波实测 | 硬件微观基础 |
| Scaling&Infra 4.6：gather-GEMM staging | 本文 §2.5 Fusion 与动态 AI | 方法同源 |
| Scaling&Infra 4.7：压缩比 Roofline 反推（融合口径） | 本文 §2.3 同构推导 | 方法互验 |
| Scaling&Infra 4.5：Sparse Selective Shrinking | 本文 §4.4 去记忆特征 | 双面同构 |
| 主线5：底层基建 | §3.3 并行选型；§4.5 EP；§4.6 传输路径 | 具体化 |

### 5.2 该系列验证了什么

三组论断得到独立来源的印证。其一，Flash Attention 是长序列 Scaling 的可行性前提——Scaling&Infra 从论文侧（主线 3）、该系列从数学侧（AI 极限 2d_h/b）推出同一结论。其二，统一交互 Block 的必要性——GEMM 统一度高的架构在 tile 层面对 SM 友好，在 AI 层面天然离拐点更近。其三，MFU 作为总账指标的地位——η 的存在说明 TCA 之类的中间指标必然失真，跨系统比较只能用 MFU。

### 5.3 该系列补充了什么

三块增量是 Scaling&Infra 没有的。**一手实测**（K=4095 对齐损失、tile 尾波阶梯、QKV 融合的动态 AI 对比）把"硬件对齐"从原则变成可执行清单；**样本范式重构的完整设计**（三级跳 + user-level training + Session Affinity）填上了"数据如何组织"这一格——主线 1 讲特征怎么进模型，此前没有讲样本怎么进训练；**去记忆特征的一手 AUC 经验**给 4.5 的 Selective Shrinking 推断提供了正向实证。

### 5.4 三处张力

**时间窗甜点 vs pre-train Sample。** Scaling&Infra 主题 2：拉长训练时间窗常无收益甚至为负，老样本特征口径漂移、ID 空间漂移，出现事实上的数据腐化。该系列：M 天聚合的 pre-train Sample 大幅降低历史重训成本。两者表面冲突，实际限制的是不同对象——甜点限制"直接喂老样本"，pre-train Sample 主张"重组后再喂"（共享序列、压缩存储）。但重组是否消除数据腐化，该系列没有给证据：序列共享解决的是 IO 冗余，不解决特征覆盖面失配。这是两份文档合流后最值得实验的问题。

**流式系统的命运。** 该系列判断流式训练系统在 Scaling Path 上持续落后，天花板是 request 级共享、多数重复序列计算无法消除——且原文明言这是"默认批式训练"假设下的推断。方向上，OneRec 恰是这个天花板的实例：SKAI 训练侧按请求 ID 分组、同一请求的多条曝光共享用户与 context 特征，系列作者的预测与业界实践同向而非对立。真正的张力在程度：OneRec 未做用户级批式重组，仍把 MFU 拉到 23.7%/28.8%，若其训练范式属流式，"持续落后"就有了公开反例——但两份文档都没有记录 OneRec 训练侧的批式/流式归属，裁决前提缺失。引用该系列这条判断时应附带此限定；"流式与长序列 Scaling 的兼容性"仍是值得单独核查的开放问题。

**口径风险。** 系列原文的数字存在口径混用：峰值算力 989/1979 是 SXM 的 dense/sparse，表格中 756/1513 是 PCIe 口径；ZeRO 扩展效率表来自公开文献与社区报告，与论文实测不可并列引用。附录 B 统一了口径。

### 5.5 合并后的行动顺序

两份文档合流后，一个推荐团队的算力兑现路径可以排成有依赖关系的序列：过精度门槛（TF32/混合精度改造，或迁移到原生支持的框架，否则一切免谈）→ 维度对齐审计（16/128 倍数、tile 凑 SM）→ 主干统一成大维度 GEMM 结构（d≥400 量级，加宽优先于加深）→ 显存换空间（FA 必装、DP+ZeRO-1/2、激活重计算按相变点权衡）→ 样本组织改造（request 共享起步，批式重组为目标形态）→ 特征减负（记忆性特征按梯度竞争证据逐类下线）→ Embedding 架构升级（EP + 热 ID 缓存 + 冷 ID offload）。每层的验收指标依次是：TCA 与精度类型、静态 AI 与拐点的距离、OOM 边界与 batch/L 上限、GPU 数据空等占比。

## 附录 A：素材来源

- 系列总览：https://zhuanlan.zhihu.com/p/1943452214958982743
- GPU 利用率与 Tensor Core：https://zhuanlan.zhihu.com/p/1943453941414236260
- 计算强度与 Roofline：https://zhuanlan.zhihu.com/p/1943473025749070170
- 显存瓶颈：https://zhuanlan.zhihu.com/p/1943482521577853368
- 系统 IO 与样本范式：https://zhuanlan.zhihu.com/p/1943596579211674175

对照引用的论断以 `rec_model_scaling_infra.md` 为准；样本范式、去记忆特征、流式系统判断等属系列原作者的观点，本文在 5.4 标注了其与 Scaling&Infra 的差异处。

## 附录 B：数值口径备忘

- 利用率指标口径：GPU Utilization（整机）、SMA（SM）、TCU/TCA（Tensor Core）与 MFU（总账）分属不同主语、不同工具口径，不可互换引用；跨系统比较只认 MFU（见 1.1）。
- H100 峰值算力：本文统一采用 SXM FP16 dense 口径 989 TFLOPS、显存带宽 3.35 TB/s，Roofline 拐点 295 FLOPs/Byte 由此得出。系列原文表格中的 756/1513 为 PCIe 版口径、1979 为 SXM sparse 口径，跨表引用需换算。
- ZeRO 扩展效率（8 卡 99%/97%/90%，128 卡 95%/90%/75%）：公开文献与社区报告口径，量级参考，与论文实测不可并列引用。
- K=4095 对齐实测在 A100（108 SM）上取得；尾波计算在 H100 SXM（132 SM）上需按 SM 数换算。
- d≥400/375 的反解依赖"BL 远大于 d"的近似与静态 AI 假设；动态 AI 更低，实际下限更高。该阈值与 M=492（batch 反解）均沿用系列原文按拐点约 300 的近似反解；严格按本文 295 口径重解则略降（d≥394/369），结论方向不变，跨文引用时须注明所用拐点。
- §2.4 的两组 batch 反解口径不同：K=128/N=384 组的 AI 上限"96"含精度字节 b=2；K=1024/N=3072 组的 AI 上限"768"不含 b（沿用系列原文），若含 b 则为 384。两组结论（batch 18/95 与 M=492/batch≈5）本身不受此口径差异影响，但引用"AI 上限"数字时需注明是否含 b。
